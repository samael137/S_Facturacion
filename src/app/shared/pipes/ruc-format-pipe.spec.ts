import { RucFormatPipe } from './ruc-format-pipe';

describe('RucFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RucFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
