import { EstadoBadgePipe } from './estado-badge-pipe';

describe('EstadoBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
